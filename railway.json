{
  "deploy": {
    "preDeployCommand": "echo 'RUN SETUP' && php artisan migrate --force -vvv && php artisan db:seed --force -vvv",
    "startCommand": "php artisan serve --host=0.0.0.0 --port=8080"
  }
}